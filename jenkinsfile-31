pipeline {
agent {label 'master'}

tools {
jdk 'Java'
maven 'Maven'
}

stages{ 
    stage('git-checkout'){
      steps{
        git branch: 'main',
        url: 'https://github.com/chinni4321/helloworld-project-1.git'
      }
    }
 }
}
